# make simple ui where it shows latest trump tweet
# and a button to create a new tweet
# deploy through lamdba. have python script that pings once per hour to the instance stays warm.
# improve lstm model
